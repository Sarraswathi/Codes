import java.sql.*;

public class JDBCDemo {
    public static void main(String[] args) throws Exception {

        String url = "jdbc:postgresql://localhost:5432/Demo";
        String name = "postgres";
        String pwd = "1022";

        Class.forName("org.postgresql.Driver");

        Connection c = DriverManager.getConnection(url, name, pwd);
        System.out.println("Program is executing");

        String sql = "UPDATE student SET marks = 76 WHERE id = 5";

        Statement st = c.createStatement();
        int status = st.executeUpdate(sql);

        System.out.println("Update operation is complete");

        st.close();
        c.close();
    }
}

import java.sql.*;

public class JDBCDemo {
    public static void main(String[] args) throws Exception {

        String url = "jdbc:postgresql://localhost:5432/Demo";
        String name = "postgres";
        String pwd = "1022";

        Class.forName("org.postgresql.Driver");

        Connection c = DriverManager.getConnection(url, name, pwd);
        System.out.println("Program is executing");

        String sql = "DELETE FROM student WHERE id = 5";

        Statement st = c.createStatement();
        int status = st.executeUpdate(sql);

        System.out.println("Delete operation is complete");

        st.close();
        c.close();
    }
}
